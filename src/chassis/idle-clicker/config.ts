import type { ChassisConfig } from '../../types';
import { THEMES } from '../../themes';

const config: ChassisConfig = {
  id: 'idle-clicker',
  name: 'Crystal Miner',
  description: 'Click to mine crystals, buy upgrades, unlock auto-miners, prestige for multipliers',
  icon: '💎',
  themes: THEMES,
  defaultTheme: 'midnight',
};

export default config;
